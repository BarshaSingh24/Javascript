"use strict";// treat all JS code as newer version

//alert(3+3)// we are using nodejs not browser so this will throw an error
let name="barsha"//string datatype
let age=22//number datatype
let isLoggedIn=false//boolean datatype

//number=>2 to power 53
//bigint=>used when the no is too large
//string=>"" or '' but prefer double quotes.